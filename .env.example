# context7 MCP — current library documentation. The [core] rule "verify versions and APIs against
# current docs, never from memory" makes it source #1. The key is OPTIONAL (it raises rate limits)
# and comes from https://context7.com/dashboard
CONTEXT7_API_KEY=
